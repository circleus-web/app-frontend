import { FormInputWithLabel } from './input-with-label';

describe('FormInputWithLabel', () => {
  it('should create an instance', () => {
    expect(new FormInputWithLabel()).toBeTruthy();
  });
});
