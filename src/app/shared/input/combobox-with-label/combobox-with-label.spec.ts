import { ComboboxWithLabel } from './combobox-with-label';

describe('ComboboxWithLabel', () => {
  it('should create an instance', () => {
    expect(new ComboboxWithLabel()).toBeTruthy();
  });
});
