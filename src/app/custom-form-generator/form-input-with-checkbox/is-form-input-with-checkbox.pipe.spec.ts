import { IsFormInputWithCheckboxPipe } from './is-form-input-with-checkbox.pipe';

describe('IsFormInputWithCheckboxPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormInputWithCheckboxPipe();
    expect(pipe).toBeTruthy();
  });
});
