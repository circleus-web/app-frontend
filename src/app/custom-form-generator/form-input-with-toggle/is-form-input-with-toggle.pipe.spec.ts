import { IsFormInputWithTogglePipe } from './is-form-input-with-toggle.pipe';

describe('IsFormInputWithTogglePipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormInputWithTogglePipe();
    expect(pipe).toBeTruthy();
  });
});
