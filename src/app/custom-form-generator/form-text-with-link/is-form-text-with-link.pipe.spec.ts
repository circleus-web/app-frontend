import { IsFormTextWithLinkPipe } from './is-form-text-with-link.pipe';

describe('IsFormTextWithLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormTextWithLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
