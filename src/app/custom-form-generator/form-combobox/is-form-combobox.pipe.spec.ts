import { IsFormComboboxPipe } from './is-form-combobox.pipe';

describe('IsFormComboboxPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormComboboxPipe();
    expect(pipe).toBeTruthy();
  });
});
