import { IsFormTextPipe } from './is-form-text.pipe';

describe('IsFormTextPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormTextPipe();
    expect(pipe).toBeTruthy();
  });
});
