import { IsFormInputWithLabelPipe } from './is-form-input-with-label.pipe';

describe('IsFormInputWithLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormInputWithLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
