import { IsFormInputWithRadioPipe } from './is-form-input-with-radio.pipe';

describe('IsFormInputWithRadioPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFormInputWithRadioPipe();
    expect(pipe).toBeTruthy();
  });
});
